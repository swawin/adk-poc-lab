# AGENTS.md

Guidelines for future coding agents:

- Keep each experiment isolated in its own folder under `experiments/`.
- Keep implementations simple and easy to read.
- Prefer minimal, self-contained setups.
- Ensure each experiment can be deployed independently.
